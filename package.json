{
  "name": "kaitodroid-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node scripts/sync-projects.mjs",
    "sync:prune": "node scripts/sync-projects.mjs --prune",
    "sync:check": "node scripts/sync-projects.mjs --dry-run"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.7.0"
  }
}
